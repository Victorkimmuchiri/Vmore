rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Anyone can read files, but only authenticated admin users can upload or delete
    match /{allPaths=**} {
      allow read: if true;
      allow write: if request.auth != null;
    }
  }
}
